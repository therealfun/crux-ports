# Description: Parallel lzip.
# URL:         http://www.nongnu.org/lzip/plzip.html
# Maintainer:  fun, just.the.real.fun at gmail dot com
# Depends on:  lzlib

name=plzip
version=1.7
release=1
source=(.watch http://download.savannah.gnu.org/releases/lzip/plzip/plzip-$version.tar.lz)

build() {
	cd */
	./configure --prefix=/usr "CXXFLAGS=$CFLAGS"
	make install-bin install-man DESTDIR=$PKG
}
