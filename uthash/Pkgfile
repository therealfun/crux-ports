# Description: A hash library implemented in C.
# URL:         https://troydhanson.github.io/uthash
# Maintainer:  fun, just.the.real.fun at gmail dot com

name=uthash
version=2.0.2
release=3
source=(
	https://github.com/troydhanson/uthash/archive/v$version/$name-$version.tar.gz
	.watch
)

build() {
	install -d $PKG/usr/include/
	install -m 0644 -t $PKG/usr/include/ */src/*.h
}
